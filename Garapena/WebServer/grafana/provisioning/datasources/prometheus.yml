apiVersion: 1

datasources:
  - name: Prometheus
    # Hace que Grafana utilice Prometheus como origen de datos por defecto
    type: prometheus
    access: proxy
    url: http://prometheus:9090
    isDefault: true
    editable: true
